module assert_router_arbiter(input logic clock, reset_n, input logic [15:0] request, grant);

//Lab4 - Bind an instance of the assert_arbiter module to port 0. Then change
//it to a macro and create 16 instances - one for each port.
//ToDo

endmodule
